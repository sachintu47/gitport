diff --git a/repository.c b/repository.c
index c7e7521..c42709f 100644
--- a/repository.c
+++ b/repository.c
@@ -325,7 +325,7 @@ int repo_submodule_init(struct repository *subrepo,
 		if (!sub) {
 			ret = -1;
 			goto out;
-		}
+	}
 
 		strbuf_reset(&gitdir);
 		submodule_name_to_gitdir(&gitdir, superproject, sub->name);
@@ -333,7 +333,7 @@ int repo_submodule_init(struct repository *subrepo,
 		if (repo_init(subrepo, gitdir.buf, NULL)) {
 			ret = -1;
 			goto out;
-		}
+	}
 	}
 
 	subrepo->submodule_prefix = xstrfmt("%s%s/",
@@ -446,7 +446,16 @@ int repo_hold_locked_index(struct repository *repo,
 			   struct lock_file *lf,
 			   int flags)
 {
+	int fd;
 	if (!repo->index_file)
 		BUG("the repo hasn't been setup");
-	return hold_lock_file_for_update(lf, repo->index_file, flags);
+	fd = hold_lock_file_for_update(lf, repo->index_file, flags);
+#ifdef __MVS__
+	struct stat st;
+	if (fd >= 0 && fstat(fd, &st) >= 0 && S_ISREG(st.st_mode)) {
+		__setfdbinary(fd);
+		__disableautocvt(fd);
+	}
+#endif
+	return fd;
 }
